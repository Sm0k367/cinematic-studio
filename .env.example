# Epic Tech AI Cinematic Studio — Environment
# Copy to .env.local for local Vite development

# These are only needed when running against real Cloudflare resources locally
# For full local simulation, the frontend works out-of-the-box with beautiful demo data.

VITE_API_BASE=/api
